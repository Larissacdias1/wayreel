# Google AI Studio — LLM + embeddings (WAYREEL.md Section 4)
GOOGLE_AI_API_KEY=

# Duffel — only once moving off MockFlightAdapter (WAYREEL.md Section 10.1, ADR-018)
DUFFEL_API_KEY=

# Server
PORT=3000
NODE_ENV=development

# Never commit the real .env — only this .env.example (WAYREEL.md Section 13 / docs/SECURITY.md)
